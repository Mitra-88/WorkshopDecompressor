name: Build Ubuntu

permissions:
  contents: read
  actions: write

on:
  push:
    branches:
      - beta
    paths:
      - '.github/workflows/**'
      - 'Src/**'
      - 'version.txt'

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4.2.2

      - name: Set up Python
        uses: actions/setup-python@v5.6.0
        with:
          python-version: '3.10.18'
          architecture: 'x64'
          cache: 'pip'

      - name: Get Shortened Commit SHA
        id: shorten_commit_sha
        uses: iendeavor/shorten-commit-sha@v2.0.4
        with:
          length: 7

      - name: Create virtual environment
        run: python3 -m venv .venv

      - name: Activate virtual environment
        run: source .venv/bin/activate

      - name: Install dependencies
        run: pip install --upgrade -r requirements.txt

      - name: Build with PyInstaller
        run: |
          pyinstaller --noconfirm --onefile --console --name "VAE" --strip --clean --optimize "2" --add-data "Src/extract_addons.py:." --add-data "Src/extract_archives.py:." --add-data "Src/utils.py:."  "Src/cli.py"

      - name: Move Bin Folder and Remove Gmad And 7-zip from Windows/macOS
        run: |
          cp -r Src/Bin dist/
          rm -f dist/Bin/Windows/fastgmad.exe
          rm -f dist/Bin/Windows/7z.exe
          rm -f dist/Bin/Darwin/fastgmad
          rm -f dist/Bin/Darwin/7z

      - name: Upload artifact
        uses: actions/upload-artifact@v4.6.2
        with:
          name: VAE-${{ steps.shorten_commit_sha.outputs.shortened_commit_sha }}-Unknow-Linux-x64
          path: dist/VAE
          compression-level: 9
